{
  "name": "facebook-manual-auto-poster",
  "version": "1.0.0",
  "description": "Manual scheduled posting dashboard for Facebook Pages using Meta Graph API.",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "node-cron": "^4.2.1"
  }
}
